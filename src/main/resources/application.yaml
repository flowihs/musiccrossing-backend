app:
  base-url: "http://localhost:8001/"

server:
  port: 8001

google:
  client-id: "${spring.security.oauth2.client.registration.google.client-id}"

spring:
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 9072551b198673
    password: 99a52af73a1aed
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:postgresql://localhost:5432/musiccrossing
    username: musicuser
    password: musicpass123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: GOOGLE_CLIENT_ID
            client-secret: #GOOGLE_CLIENT_SECRET
            scope: openid, profile, email
            redirect-uri: "{baseUrl}/login/oauth2/code/google"
        provider:
          google:
            issuer-uri: https://accounts.google.com
    user:
      name: admin
      password: admin123

logging:
  level:
    ru.github.musiccrossing: DEBUG
    org.springframework.security: DEBUG

jwt:
  secret: "music-crossing-jwt-secret-key-2024-32bytes"
  access-expiration: 3600000
  refresh-expiration: 2592000000

telegram:
  bot-token: "8563803834:AAFPz2KwyThJ2qnrT0YyRqdXwriAXObHbFs"


