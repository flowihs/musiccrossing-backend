app:
  base-url: "http://localhost:8001/"
  s3-bucket: "musiccrossing"

server:
  port: 8001

google:
  client-id: "${spring.security.oauth2.client.registration.google.client-id}"

spring:
  cloud:
    aws:
      credentials:
        access-key: "81827bbda8e42f7367fa8353f29e0d23"
        secret-key: "d43c9a1c930cf533f9f97e6206696960b2f25a7422c19dca6628b8e064071da1"
      region:
        static: "eu-west-1"
      s3:
        endpoint: "https://zellhmmwecyanmnvmdlt.storage.supabase.co/storage/v1/s3"
  mail:
    host: sandbox.smtp.mailtrap.io
    port: 2525
    username: 9072551b198673
    password: 99a52af73a1aed
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
  datasource:
    url: jdbc:postgresql://localhost:5432/musiccrossing
    username: musicuser
    password: musicpass123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true

  security:
    oauth2:
      client:
        registration:
          google:
            client-id: YOUR_GOOGLE_CLIENT_ID
            client-secret: YOUR_GOOGLE_CLIENT_SECRET
            scope: openid, profile, email
            redirect-uri: "{baseUrl}/login/oauth2/code/google"
        provider:
          google:
            issuer-uri: https://accounts.google.com
    user:
      name: admin
      password: admin123

logging:
  level:
    ru.github.musiccrossing: DEBUG
    org.springframework.security: DEBUG

jwt:
  secret: "your-super-long-secret-key-at-least-32-characters"
  access-expiration: 3600000
  refresh-expiration: 2592000000

telegram:
  bot-token: "your_telegram_bot_token_here"